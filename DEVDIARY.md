***2026年2月4日***

はじめまして、VilliDoug「ギィ」と申します。

昨年よりWebアプリケーション開発を学習しており、今年1月から転職活動開始いたしました。<br>
コースの先輩方に **「アプリをどんどん作りましょう」** という声に、ボランティア活動中のブラジリアン柔術ジム管理用アプリケーションを開発しようと思いました。

規模は小さいけど、私自身は今年そのジムで指導者として5年目に入ります。<br>
スタッフの方々の中、情報共有を上手くできず、マネジメントのことに不安があります。

そのため、まずは**一人で本アプリを実装し、実際に使うつもりです。**<br>
 ≪私は現在週1日しか参加できないため、土曜日のことを全て本アプリで対応できればと思っています。≫

#### 1. 「MVP」な状態で使用開始

ジムで**よくある問題**はこちら:

 - 会員によって、一月の参加回数が決まっている。**現在は当日の担当インストラクターがノートに参加した会員名を書き**、管理しています。
 - あまり会わない会員が参加すると、**名前が分からない**ケース
 - 毎月代表者がカウントして、Excelスプレッドシートに入力しているそうです「時間の無駄」。
 - 去年10月から、代表者が仕事の都合のために、ジムまで4ヶ月ほど来れてない　＝　**管理者いない。**
 - ボランティアスタッフに任せようとしていますが、**私たちは情報不足で管理できません。**
 - 支払い管理は特に大変になっています。PayPalを優先にしていますが、**使用経験がない会員が多く**、<br>個人的にPayPay・他コード払いにしたら良いでしょうと思い、**将来的にQR払いができるようにしてあげたい。**
 - ジムの**公式サイトに古い情報があり、ビジターや体験者に正しい情報**を出してあげたい。

他のもたくさんあるはずが、これから丁寧にアプリを実装・試し・機能追加をしていきたいと思います。

私は現在正社員として務めて、転職活動をやりながら作成するかたちになりますので、設計・実装・テスト・管理を全て一人でやるつもりですが<br>
家庭の都合や仕事の都合により、AIのサポートを受けながら少しでも早く実装へ。

#### 2.　【Claude・Gemini】 - AI使い方について<br>

- AIのコードを貼り付けのではなく、考えていることについて相談相手として使用するつもりです。<br>
- ファイル設計・ベストプラクティス・命名規則・設定ファイル等々にサポートをもらう。
- CSSスタイリングをバランスよくする。
- コードを読みやすくする等

#### 3. 技術スタック

Java・SpringBoot・JPA・MySQL・TypeScript・ReactかNext.js（検討中）

---

***2026年2月5日***

昨日の日報に書き忘れました:
- JPAの力で、アプリを実行する時にDBテーブルを作ってくれました。
- "members" テーブルに3つのサンプルエントリーを追加。
- 再実行してブラウザーで　【localhost:8080/api/members】 で動作確認。

**今日は:**

#### 1. DTO　実装

**APIエンドポイントに渡すDTOを作り、ServiceでDBデータを扱ってDTOに。**

- メリットはフロントで使いたい情報だけを扱う。
- 会員情報と参加回数情報を　⇒　会員詳細情報。
- 将来的に「Admin,Staff」のユーザーに対して、
  - 「Admin」は会員詳細情報を全て表示・編集可。
  - 「Staff」の場合は参加回数の編集、会員詳細情報のみ表示。

#### 2. MVCによる　Repository　→　Service　→　Controller　の実装・動作確認

GET HTTPメソッドを実装し、現在のエンドポイントは

- /api/members　- 会員一覧（MemberDTO）
- /api/members/{id}/details　- 会員詳細情（IDで取得したMemberDetailDTO）

Claudeに相談をしながら、エンドポイント名のベストプラクティス、DTOオブジェクト、JPAの使い方（JPA初使用）。
最近Javaに触れていないため、StreamAPIの書き方を復習して実装しました。

参加回数「スタッフの中で　・クラスカウント・」をMembershipType(enum)によって一人のリミットがあって、</br>
できれば自動的に毎月リセットしてほしいですが、誰かが確認をするまで時間がたつ可能性があって、リセットボタンを実装したいと思います。</br>
もしくは、アプリ側でカレンダーの確認をしたあと、カウントをリセットし、前月のデータをアクセスできるように、検索機能を実装する。

楽しい。

次は　POST・PUT　HTTPメソッド。

---

***2026年2月6日***

#### 1. オブジェクト修正

Member　→　lineId　追加しました。
- ジムでは会員との連絡・運営は全てLINEでやっているので、LINE　IDが必要。

MemberDTO ・　MemberDetailDTO　バリエーション(@NotBlank / @NotNull)実装しました。
- 「POST・PUT」新規会員登録の時バリエーション機能を実装。

#### 2. MemberMapper

Serviceコードが長くて、そのままにしていくとコードが読みにくく、早いうちにMapperを実装しました。
- Mapperクラス
 - Entity ⇐⇒　DTO　のコンバーターメソッドを実装。 

#### 3.　POST　メソッド

@Transactional アノテーションを使用したPOSTメソッドを実装。

Postmanを使って、【GET・GET（ID）・POST】の動作確認済み！

GET成功した場合　"200"　（ResponseEntity.ok）、
POST成功した場合　"201"　（ResponseEntity.status(HttpStatus.CREATED)) →　初めて。

例外処理は今度から実装する予定です。

今日の日報は短いですが、明日また頑張ります！

---

***2026年2月8日***

7日は家族サービスデイでした。

#### 1. クラスの修正

Memberに新しいフィルドを追加しました。
- alphabetName
  - たまには漢字読めなくて…外国人スタッフがいる存在をして追加しました。
- boolean staff
  - 当日の担当インストラクターを指定できるために。
- isActive → active
  - Lombok機能で、Postmanで動作確認をする時にエラーが発生しました。
    - 読み取り: isStaff() を呼び出す → JSON は "isStaff": false になる。
    - 書き込み: setStaff() を探す → JSON は "staff": false になる。
    - 新規登録の時、active = trueを行う。

「getActiveMembers」を使って条件検索を行う。

#### 2. PUT・DELETEメソッド実装

- updateMemberメソッド実装。
- deleteMemberメソッド実装。
  -  論理削除を行う。

#### 3. 参加の管理

機能は一旦:

- その当日の参加者を確認できるメソッド。
  - フロントエンドで「参加」ボタンを押した後、その会員は「本日の参加者リスト」に表示を目的として実装。
- 「参加」イベントをそのメンバーに付けるメソッド。
  - 会員:参加 (Member : Attendance) は　1 : 多。

CRUD処理はとりあえず実装できています！
今週は火曜日までしかパソコンに触れなくて、フロントエンドの環境構築の準備と、時間があればランディングページを実装したい。

---

***2026年2月9日***

#### 1. MVP・バックエンド完成

- removeAttendanceメソッドを実装して、一旦 MVP (Minimum Viable Product)として使用できるようになっている　**☆はず。☆**
  - AttendanceDTOを作るより、検索条件機能を上手く使って、会員IDと日付でフィルターをして削除できるメソッドを実装。

#### 2. フロントエンドの環境構築

React Viteにしました。

Next.jsはとても良いと思いますが、Reactを使いっこなした方がJavaScript・TypeScriptの書き方がうまくなると思うし、求人もReactの方がたくさんありますので…

そして、本アプリは小規模で、バックエンドはSpringBootで用意しているので、Next.jsを使わないことを決定しました。

日曜日まで開発停止になりますが、来週は一生懸命頑張ります！

---

***2026年2月15日***

東京から帰ってきました！

#### 1. TypeScriptへの移行

コードの書きやすさかつ読みやすさを大切にして、TypeScriptを使用することにしました。

フロントエンドコースを受講中にも、TypeScriptの方が確実に理解しやすかった。

#### 2. SASS実装　「CSSプリプロセッサー」

TailWindCSSに似たようなUIフレームワークを使用せず、CSSをもっと練習したいと思い、</br>
ネットで何度もみたSASSを使うことにしました。</br>

QoL「Quality of Life」を大切にし、苦手なCSSをすこし楽にしてくれると助かります！

SASSをコンパイラーとして使用し、.scssファイルで実装。
.scssのメリットは例えば変数、ネスティング、＠if・＠for文などを実装できること。

CSSは苦手だが、これでプログラミングをやっている感アップ！

#### 3. フロントエンドTypesの実装・複数な関数の実装

MemberDTO, MemberDetailDTOを実装。

バックエンドと同じタイプを作り、データ取得する際、表示ができるようにしました。</br>
APIエンドポイントから取得できるために、memberService / attendanceServiceも実装しました。

何度もapiエンドポイントを書くより、関数を呼び出すだけでコードが読みやすくなります。

#### 4. 会員一覧表示「確認済み」

そして何時間後に、やっとブラウザーで画面表示ができて、スタイリングを修正しながらキレイに当たって嬉しい！

明日は検索機能の実装や編集機能の実装などをやりたいと思います。

#### 余談

5日間でも学習しないと忘れていくことにショック！

最近はコードを見て理解はしやすくなってきましたが、自分からコードを書いていくのが大変。
でも、やりたいことがきちんと決めていて、どうやってコードに変換をしていくことを検索・AIにサポートを受け</br>
**必ず手でコードを打っていくこと**にしてます。

フロントエンドを書くのにすぐ何百行になりますが、頑張ります！

---

***2026年2月16日***

⇒ 昼間

#### 1. 検索機能を実装しました。

以前似たようなコードを書いたことがあって、そのプロジェクトを参考にしながら実装してみました。

しかし、答え合わせをするところで、新しい書き方を拝見して、そちらにしてみました「コードが読みやすくなりました」

#### 2. Layout.tsxとLayout.scssの実装。

1時間半頑張りましたが、基本的にCSSコードを書いてました。

しつこいスタイリングバッグもありますが、20分ほど解決できず、今度からデバッグするつもりです。

#### 3. カスタムフック

フロントエンドの少し嫌なところはコードが長くて読みづらい。

カスタムフックを実装するとコードが読みやすく、理解しやすくなります。

MembersListPageと新しいTodaysClassPageもカスタムフックを使用しています。

⇒ フロントエンドに興味アップ。
難しいというか、文法が面白いというか、理由は特に分かりませんが...
画面表示で細かいところまで修正できるが一つの理由かもしれません。

---

***2026年2月17日***

目の調子が悪いため日報をまとめて書きます。

クラスの参加を管理できるページを実装しました。
- 会員のカードに一度クリックしてセレクト、そのあと「出席」ボタンが表示し、押すと「本日の参加」リストに会員が入ります。

細かいところもたくさんありますが、日報はここまでで。

---

***2026年2月25日***

最近バタバタして、帰ったらすぐ寝落ちる...

#### 1. 参加管理ページを実装

当日の参加者を管理できる機能を実装しました。

- 当日のスタッフをセレクト、
- 会員カードをクリック、参加ボタンを押して本日の参加リストに追加、
  - 間違って追加した場合、削除ボタンを実装しました。

#### 2. 支払方法を考慮

PaymentMethodクラスを実装。

今のところはただMANUAL「現金払い」・AUTOMATIC「自動払い」だけにしています。

#### 3. 月々のデータ

MonthlyReportクラスを実装し、そのクラスは月々のデータを扱うクラスで、</br>
会員の参加回数、支払状況などをまとめて保存・表示するために作りました。

---

